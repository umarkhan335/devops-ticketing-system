version: "3.8"

services:
  web:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - redis-db

  redis-db:
    image: redis:alpine
    volumes:
      - redis_data:/data

volumes:
  redis_data:
